<?php

/**
 * Application configuration
 *
 * @var     array
 */
return array(

	/**
	 * The base URL of your application
	 *
	 * Set this to an empty value to have it automatically detected.
	 *
	 * @var     string
	 */
	'url' => null,

	/**
	 * Any suffix that needs to be added to URL's generated by Atom. If the
	 * suffix is an extension such as ".html", be sure to include the "."
	 * character.
	 *
	 * @var     string
	 */
	'url_suffix' => false,

	/**
	 * The name of your main bootstrapping file. If you would like to remove the
	 * filename from your URLs, and are using mod_rewrite or a similar
	 * equivelant, you can set this as an empty string.
	 *
	 * @var     string
	 */
	'index' => 'index.php',

	/**
	 * Application environment.
	 *
	 * This value is used for environmentally configurable values, such as
	 * database connectivity.
	 *
	 * @var     string
	 */
	'environment' => 'development',

	/**
	 * The default timezone of your application. This timezone will be used
	 * when Atom needs to generate a date, such as when writing to a log file.
	 *
	 * [!!] This is extremely important to your application. Since PHP 5.1.0
	 *      (when the date/time functions were rewritten), every call to a
	 *      date/time function will generate a E_NOTICE if the timezone isn't
	 *      valid, and/or a E_WARNING message if using the system settings or
	 *      the TZ environment variable.
	 *
	 * @see     http://php.net/timezones
	 * @var     string
	 */
	'timezone' => 'UTC',

	/**
	 * The default character encoding.
	 *
	 * @var     string
	 */
	'encoding' => 'UTF-8',

);

/* End of file application.php */